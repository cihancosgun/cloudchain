export declare enum FileType {
    file = 0,
    dir = 1,
}
export interface TreeNodeParams {
    name: string;
    type?: FileType;
    children?: Array<TreeNodeParams>;
    focus?: boolean;
}
export declare class TreeNode {
    name: string;
    type: FileType;
    children: Array<TreeNode>;
    private fullFilepath;
    private parentNode;
    private _isFocused;
    private _isExpanded;
    constructor(params: TreeNodeParams, parent?: TreeNode);
    getFullPath(): string;
    isDir(): boolean;
    getParentNode(): TreeNode;
    isExpanded(): boolean;
    expand(): void;
    fold(): void;
    hasParent(): boolean;
    focus(): void;
    blur(): void;
    stringify(): string;
}
