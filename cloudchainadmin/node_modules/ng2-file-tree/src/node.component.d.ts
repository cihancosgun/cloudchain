import { EventEmitter } from '@angular/core';
import { TreeNode } from './tree-node';
export declare class NodeComponent {
    node: TreeNode;
    index: number;
    clicked: EventEmitter<TreeNode>;
    constructor();
    isExpandable(): boolean;
    isExpanded(): boolean;
    expandFolder(): void;
    clickItem(node: TreeNode): void;
    propagate(node: TreeNode): void;
}
