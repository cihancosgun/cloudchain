import { EventEmitter } from '@angular/core';
import { ElementRef } from '@angular/core';
import { SimpleChanges } from '@angular/core';
import { OnInit } from '@angular/core';
import { OnChanges } from '@angular/core';
import { TreeNode } from './tree-node';
export declare class FileTreeComponent implements OnInit, OnChanges {
    private _eref;
    tree: TreeNode;
    keyboardWatch: boolean;
    onChange: EventEmitter<TreeNode>;
    private root;
    private currFocusNode;
    constructor(_eref: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    fileTreeClicked(nextNode: TreeNode): void;
    keydownHandler(event: KeyboardEvent): void;
    private updateFocusNode(next);
}
